/**
 * Optional content seed only. Demo login accounts are not created.
 * Existing MongoDB users are never deleted or overwritten here.
 */
const ensureDevAccounts = async () => {
  // Intentionally empty — use real school accounts in Atlas / local MongoDB.
};

module.exports = { ensureDevAccounts };
